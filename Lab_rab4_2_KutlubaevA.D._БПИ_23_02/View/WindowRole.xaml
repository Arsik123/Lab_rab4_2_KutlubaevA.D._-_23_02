<Window x:Class="Lab_rab4_2_KutlubaevA.D._БПИ_23_02.View.WindowRole"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_rab4_2_KutlubaevA.D._БПИ_23_02.View"
        mc:Ignorable="d"
        Title="Должности"
        Height="300" Width="300">

    <DockPanel Background="#FFD8D8D8">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Добавить" Width="70" Margin="5" Command="{Binding AddRole}"/>
            <Button Content="Редактировать" Width="90" Margin="5" Command="{Binding EditRole}"/>
            <Button Content="Удалить" Width="70" Margin="5" Command="{Binding DeleteRole}"/>
        </StackPanel>

        <ListView x:Name="lvRole" Background="#FFD3CDCD" ItemsSource="{Binding ListRole}" SelectedItem="{Binding SelectedRole, Mode=TwoWay}" Margin="5">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Код" Width="50" DisplayMemberBinding="{Binding Id}" />
                        <GridViewColumn Header="Наименование должности" DisplayMemberBinding="{Binding NameRole}" />
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
