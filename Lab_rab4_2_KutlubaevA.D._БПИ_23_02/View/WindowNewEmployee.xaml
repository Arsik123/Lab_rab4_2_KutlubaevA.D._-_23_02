<Window x:Class="Lab_rab4_2_KutlubaevA.D._БПИ_23_02.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сотрудник" Height="260" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Код:</Label>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Id}" VerticalAlignment="Center"/>

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,0,0">Должность:</Label>
        <ComboBox x:Name="CbRole"
                  Grid.Row="1" Grid.Column="1" Margin="0,6,0,0"
                  DisplayMemberPath="NameRole" />

        <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,0,0">Фамилия:</Label>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,6,0,0"
                 Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,0,0">Имя:</Label>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,6,0,0"
                 Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,0,0">Дата рождения:</Label>
        <DatePicker Grid.Row="4" Grid.Column="1" Margin="0,6,0,0"
                    SelectedDate="{Binding Birthday, Mode=TwoWay}" />

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Width="75" Margin="6,0,0,0" IsDefault="True" Click="Ok_Click">OK</Button>
            <Button Width="75" Margin="6,0,0,0" IsCancel="True">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>
